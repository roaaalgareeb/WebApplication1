@model IEnumerable<Recipe_Task.Models.Testimonial>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Rating)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Testi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Testi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailNavigation.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TestimonialId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TestimonialId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TestimonialId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
